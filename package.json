{
    "name": "mercury-fleet-api-root",
    "version": "1.0.0",
    "scripts": {
        "build": "cd backend && npm install && npm run build",
        "start": "cd backend && npm run start:prod",
        "heroku-postbuild": "cd backend && npm install && npx prisma generate && npm run build"
    },
    "engines": {
        "node": ">=18.0.0",
        "npm": ">=9.0.0"
    }
}